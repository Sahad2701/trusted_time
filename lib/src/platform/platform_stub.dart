import '../../trusted_time_platform_interface.dart';

/// Stub function to satisfy the conditional import requirements.
TrustedTimePlatform getPlatformInstance() => throw UnimplementedError(
      'Cannot create a platform instance without specialized implementation.',
    );
